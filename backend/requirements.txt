# Golden Hour backend dependencies
# Python 3.10+ required (uses built-in union types and dict[str, Any] syntax)

# Web framework
fastapi==0.128.0
starlette==0.50.0
uvicorn==0.40.0

# Database
SQLAlchemy==2.0.46
alembic==1.18.1
# psycopg2-binary: pre-compiled PostgreSQL driver. Works on Windows/Mac/Linux
# without needing pg_config or PostgreSQL dev headers installed.
# Do NOT swap to psycopg2 (the non-binary variant) in development — it requires
# system-level PostgreSQL headers that are painful to install on Windows.
psycopg2-binary==2.9.11

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Pydantic (data validation & settings)
pydantic==2.12.5
pydantic-settings==2.12.0
pydantic_core==2.41.5
email-validator==2.1.1

# Environment / config loading
python-dotenv==1.2.1

# Redis client (installed so it's available; active caching is a future feature)
redis==7.1.0

# Rate limiting (brute-force protection on auth + admin endpoints)
slowapi==0.1.9
limits==5.8.0

# Structured logging
loguru==0.7.2

# Transitive dependencies (pinned for reproducibility across Mac and Windows)
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
click==8.3.1
colorama==0.4.6
greenlet==3.3.0
h11==0.16.0
idna==3.11
Mako==1.3.10
MarkupSafe==3.0.3
packaging==26.0
typing-inspection==0.4.2
typing_extensions==4.15.0
